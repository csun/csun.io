# Site settings
title: Cameron Sun
description: > # this means to ignore newlines until "baseurl:"
  The personal blog of Cameron Sun, a software engineer who lives in Seattle.
baseurl: "" # the subpath of your site, e.g. /blog/
url: "https://www.csun.io" # the base hostname & protocol for your site

author: Cameron Sun

plugins:
  - jekyll-redirect-from
  - jekyll-feed

# Entries listed on the homepage that live elsewhere. output: false keeps them
# out of the built site and the feed; they exist only as listing metadata.
collections:
  links:
    output: false

permalink: /:title

# Build settings
markdown: kramdown
highlighter: rouge
kramdown:
  syntax_highlighter: rouge

sass:
  sass_dir: _sass
